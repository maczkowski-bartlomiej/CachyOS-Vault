#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=theme-build-lib
source "$SCRIPT_DIR/theme-build-lib"
init_theme_builder

OUTPUT_FILE="$THEME_OUTPUT_DIR/i3-theme.i3"
FOCUSED_BORDER="$ACCENT"
FOCUSED_BG="$BG_ALT"
UNFOCUSED_BORDER="$BG"
UNFOCUSED_BG="$BG"
INACTIVE_BORDER="$BG_ALT"
INACTIVE_BG="$BG"
PLACEHOLDER="$BLACK"

write_file_atomic "$OUTPUT_FILE" <<I3
# Generated by CachyOS-Vault. Do not edit directly.
# Source palette: custom-themes/color-palette/orchis-dark-palette.sh

set \$bg                 $BG
set \$bg_alt             $BG_ALT
set \$surface            $SURFACE
set \$surface_2          $SURFACE_2
set \$surface_3          $SURFACE_3
set \$fg                 $FG
set \$fg_strong          $FG_STRONG
set \$fg_dim             $FG_DIM
set \$fg_muted           $FG_MUTED
set \$fg_disabled        $FG_DISABLED
set \$border             $BORDER
set \$border_soft        $BORDER_SOFT
set \$accent             $ACCENT
set \$accent_hover       $ACCENT_HOVER
set \$accent_active      $ACCENT_ACTIVE
set \$accent_dim         $ACCENT_DIM
set \$success            $SUCCESS
set \$warning            $WARNING
set \$error              $ERROR
set \$urgent             $URGENT
set \$primary            $PRIMARY
set \$secondary          $SECONDARY
set \$focused_border     $FOCUSED_BORDER
set \$focused_bg         $FOCUSED_BG
set \$inactive_border    $INACTIVE_BORDER
set \$inactive_bg        $INACTIVE_BG
set \$unfocused_border   $UNFOCUSED_BORDER
set \$unfocused_bg       $UNFOCUSED_BG
set \$placeholder        $PLACEHOLDER
I3
log_ok "Generated $OUTPUT_FILE"
